model public_files {
  id        Int      @id @default(autoincrement())
  name      String   @unique
  size      Int
  upload_at DateTime @default(now())
  flowers   flowers_and_files[]
}

model private_files {
  id        String   @id
  name      String   @unique
  size      Int
  upload_at DateTime @default(now())
}